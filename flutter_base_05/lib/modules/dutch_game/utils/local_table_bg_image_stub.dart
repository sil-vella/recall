import 'package:flutter/material.dart';

bool localTableBackGraphicCached(String absolutePath) => false;

Widget localTableBgImageFile(String absolutePath, {BoxFit fit = BoxFit.cover}) =>
    const SizedBox.shrink();
